Options -MultiViews
RewriteEngine On

# Serve existing files and directories as-is
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Fallback: all unmatched paths → index.html (SPA routing)
RewriteRule ^ index.html [QSA,L]
